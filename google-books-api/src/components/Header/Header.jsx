import React from 'react';
import styles from "./Header.module.scss"

const Header = () => {
    return (
        <div>
            <h1>Google Book Search</h1>
            <h2>Look up a new book or an old favourite in the search bar below!</h2>
        </div>
    );
};

export default Header;